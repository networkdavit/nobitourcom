import { TooltipFormatPipe } from './tooltip-format.pipe';

describe('TooltipFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TooltipFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
